## Практикум для решение задач на языке GO.
### Название практикума по которому решаем задачу

#### 01. Тестовое задание from 14.02.25
```
// Необходимо написать нечто для хранения данных, соответствующее этому интерфейсу

// add(x)       - add element `x` to the structure
// get_unique() - return some element that is uniquely represented in the structure
// delete(x)    - remove element `x` from the structure
```
